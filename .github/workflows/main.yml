
name: Docker CI

on:
  push:
    branches:
      - main

jobs:
  docker-ci:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout App Repo
        uses: actions/checkout@v4

      - name: Configure Docker for insecure Harbor registry
        run: |
          sudo bash -c 'cat > /etc/docker/daemon.json <<EOF
          {
            "insecure-registries": ["harbor-inventyv.duckdns.org:30001"]
          }
          EOF'
          sudo systemctl daemon-reload
          sudo systemctl restart docker
          echo "Docker configured for insecure registry"


      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          registry: harbor-inventyv.duckdns.org:30001
          username: ${{ secrets.USERNAME_HARBOR }}
          password: ${{ secrets.TOKEN_DOCKER_HARBOR }}

      - name: Build Docker Image
        run: |
          docker build -t harbor-inventyv.duckdns.org:30001/react-app:${{ github.sha }} ./my-react-app

      
      - name: Push Docker Image
        run: |
          docker push harbor-inventyv.duckdns.org:30001/react-app:${{ github.sha }}

      
      - name: Clone Timoni Repo
        run: |
          git clone https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/meetchhatrala/timoni-repo.git

     
      - name: Show values.cue before update
        run: |
          cd timoni-repo
          echo "Before update:"
          cat values.cue

      
      - name: Update image tag in values.cue
        run: |
          cd timoni-repo
          IMAGE_TAG=${{ github.sha }}

          sed -i "s/tag:[[:space:]]*\".*\"/tag:        \"$IMAGE_TAG\"/" values.cue

          echo "After update:"
          cat values.cue

      
      - name: Commit and push changes
        run: |
          cd timoni-repo
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"

          git add values.cue

          if git diff --cached --quiet; then
            echo "No changes detected"
          else
            git commit -m "Update image tag to ${{ github.sha }}"
            git push https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/meetchhatrala/timoni-repo.git HEAD:main
          fi
